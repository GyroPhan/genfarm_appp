import 'package:flutter/material.dart';

class Ata32EcamScreen extends StatelessWidget {
  const Ata32EcamScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
