import 'package:flutter/material.dart';

class DicMenuScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('COMMING SOON'),
      ),
    );
  }
}
