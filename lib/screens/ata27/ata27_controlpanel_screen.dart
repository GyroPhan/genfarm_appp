import 'package:flutter/material.dart';

class Ata27ControlPanelScreen extends StatelessWidget {
  const Ata27ControlPanelScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
